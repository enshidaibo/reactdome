import ContentLayout from '@/pages/A_Layout/ContentLayout';

const ChannelIndex = ({ children }) => {
    return (
        <ContentLayout>
            {children}
        </ContentLayout>
    )
}

export default ChannelIndex