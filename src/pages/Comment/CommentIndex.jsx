import ContentLayout from '@/pages/A_Layout/ContentLayout';

const CommentIndex = ({ children }) => {
    return (
        <ContentLayout>
            {children}
        </ContentLayout>
    )
}

export default CommentIndex